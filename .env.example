# Football Data API (https://www.football-data.org/) — free tier, 10 req/min
FOOTBALL_DATA_API_KEY=

# TheSportsDB API (https://www.thesportsdb.com/) — free or premium ($9/mo for 10 events)
SPORTSDB_API_KEY=

# PostgreSQL connection string (Neon serverless) — only needed for drizzle db:push
# Currently unused at runtime; Worker uses in-memory cache
DATABASE_URL=
